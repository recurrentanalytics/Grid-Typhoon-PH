"""Hotspot statistics stage (to be lifted from the canonical notebook)."""

from __future__ import annotations

from kritis_ph.config import RunConfig


def run(cfg: RunConfig) -> None:
    raise NotImplementedError(
        "hotspot_stats: lift hotspot analysis cells from "
        "KRITIS_PH_canonical_notebook_commented.ipynb into this module."
    )
